# Glitch-Assets-Downloader
Downloads files from .glitch-assets file and saves them

Note: must have a .glitch-assets file in the right format

Download `axios` and `progress` from
```
npm i axios progress
```

run the code using 

```
node index.js
```
